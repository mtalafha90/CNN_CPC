"""A solution pipeline for the RSNA Knee Abnormality Detection challenge.

The package is deliberately schema-agnostic: label names, identifier columns
and the submission layout are all discovered at run time from the competition
CSV files, so the same code works no matter how the organisers name things.
"""

__version__ = "0.1.0"
