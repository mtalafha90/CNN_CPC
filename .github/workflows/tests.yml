name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install package and tests
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest
      - name: Compile Python sources
        run: python -m compileall -q src tests kaggle
      - name: Verify dependencies
        run: pip check
      - name: CLI/runtime import smoke
        run: rsna-knee runtime --config configs/train.yaml
      - name: Run tests
        env:
          WORLD_SIZE: '1'
        run: pytest -q
